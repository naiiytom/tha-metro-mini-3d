node_modules/
dist/
rust-engine/target/
*.tsbuildinfo
*.local
.DS_Store
.superpowers/
.worktrees/

# Namtang GTFS feed, fetched on demand by tools/fetch-gtfs.mjs — ~230 MB
# extracted, dominated by shapes.txt/fare_*.txt the preprocessor never reads.
# Never committed; re-download instead of trusting a stale local copy.
.gtfs-cache/
